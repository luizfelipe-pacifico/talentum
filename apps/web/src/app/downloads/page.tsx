import { redirect } from 'next/navigation'; import { cloud } from '../../server/cloudflare';
type Release={id:string;version:string;channel:string;artifactId:string;platform:string;architecture:string;sha256:string};export const dynamic='force-dynamic';
export default async function Page(){const me=await cloud('/api/me',{},true);if(!me.ok)redirect('/api/auth/start');const result=await cloud('/api/releases',{},true),items=result.ok?(await result.json() as {releases:Release[]}).releases:[];return <main style={{maxWidth:880,margin:'64px auto',padding:24}}><h1>Downloads do Talentum</h1><p>Escolha sua plataforma e confira o SHA-256 antes de executar.</p>{items.length===0?<p>Nenhuma versão publicada está disponível.</p>:items.map(x=><article key={x.artifactId} style={{border:'1px solid #d8d0c5',padding:20,marginTop:16}}><h2>{x.platform} · {x.architecture}</h2><p>Versão {x.version} ({x.channel})</p><code style={{overflowWrap:'anywhere'}}>{x.sha256}</code><form action={`/api/releases/${x.id}/download-grants`} method="post"><input type="hidden" name="artifactId" value={x.artifactId}/><button type="submit">Baixar</button></form></article>)}<form action="/api/auth/logout" method="post"><button type="submit">Sair</button></form></main>}
